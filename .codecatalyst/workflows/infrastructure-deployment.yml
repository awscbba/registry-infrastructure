Name: Infrastructure_Deployment_Pipeline
SchemaVersion: "1.0"

Triggers:
  - Type: PUSH
    Branches:
      - main
  - Type: PULLREQUEST
    Branches:
      - main
    Events:
      - OPEN
      - REVISION
  - Type: MANUAL

Actions:
  CheckAPISync:
    Identifier: aws/build@v1
    Compute:
      Type: EC2
    Environment:
      Connections:
        - Role: CodeCatalystWorkflowDevelopmentRole-AWSCocha
          Name: "142728997126"
      Name: AWS
    Inputs:
      Sources:
        - WorkflowSource
    Configuration:
      Steps:
        - Run: |
            echo "🔍 Checking API Code Synchronization"
            echo "==================================="
            echo "📅 Stage Start: $(date -u +%Y-%m-%dT%H:%M:%SZ)"
            echo ""

            # Set execution mode based on trigger
            if [ "${CODECATALYST_TRIGGER_TYPE}" = "PULLREQUEST" ]; then
                export EXECUTION_MODE="validation"
                export SKIP_DEPLOYMENT="true"
                echo "🎯 Mode: validation (PR feedback)"
            else
                export EXECUTION_MODE="deployment"
                export SKIP_DEPLOYMENT="false"
                echo "🎯 Mode: deployment (main branch)"
            fi

            # Check for API sync
            if [ -f "lambda/main.py" ] && [ -d "lambda/src" ]; then
                echo "✅ API code synchronized from registry-api repository"
                API_SYNC_DETECTED="true"
            else
                echo "ℹ️ No API sync detected - using existing lambda handlers"
                API_SYNC_DETECTED="false"
            fi

            # Create deployment context
            echo "{" > deployment-context.json
            echo "  \"execution_mode\": \"$EXECUTION_MODE\"," >> deployment-context.json
            echo "  \"api_sync_detected\": \"$API_SYNC_DETECTED\"," >> deployment-context.json
            echo "  \"skip_deployment\": \"$SKIP_DEPLOYMENT\"," >> deployment-context.json
            echo "  \"timestamp\": \"$(date -u +%Y-%m-%dT%H:%M:%SZ)\"" >> deployment-context.json
            echo "}" >> deployment-context.json

            echo "✅ API sync check completed"
      Container:
        Registry: CODECATALYST
        Image: CodeCatalystLinux_x86_64:2024_03
    Outputs:
      Artifacts:
        - Name: deploymentContext
          Files:
            - "deployment-context.json"

  ValidateInfrastructure:
    Identifier: aws/build@v1
    DependsOn:
      - CheckAPISync
    Compute:
      Type: EC2
    Environment:
      Connections:
        - Role: CodeCatalystWorkflowDevelopmentRole-AWSCocha
          Name: "142728997126"
      Name: AWS
    Inputs:
      Sources:
        - WorkflowSource
      Artifacts:
        - deploymentContext
    Configuration:
      Steps:
        - Run: |
            echo "⚡ Infrastructure Validation"
            echo "=========================="
            echo "📅 Stage Start: $(date -u +%Y-%m-%dT%H:%M:%SZ)"
            echo ""

            # Read execution mode from context
            EXECUTION_MODE=$(jq -r '.execution_mode' deployment-context.json)
            echo "🎯 Mode: $EXECUTION_MODE"
            echo ""

            # CDK Synthesis Check with Node.js warning handling
            echo "🔄 CDK Synthesis Check..."

            # Install CDK and dependencies (use older version that supports Node 18)
            npm install -g aws-cdk@2.60.0 > /dev/null 2>&1
            python3 -m venv .venv > /dev/null 2>&1
            source .venv/bin/activate
            pip install -r requirements.txt > /dev/null 2>&1

            # Suppress Node.js end-of-life warnings for CDK
            export CDK_DISABLE_VERSION_CHECK=1
            export NODE_NO_WARNINGS=1

            # Run CDK synthesis with intelligent error handling
            echo "Running CDK synthesis..."
            cdk synth > cdk-synth-output.txt 2>cdk-synth-errors.txt
            CDK_EXIT_CODE=$?

            # Check if the failure is just due to Node.js warnings
            if [ $CDK_EXIT_CODE -eq 0 ]; then
                echo "✅ CDK synthesis successful - Infrastructure is valid"
                VALIDATION_STATUS="success"
            elif grep -q "Node.*has reached end-of-life" cdk-synth-errors.txt; then
                echo "⚠️ Node.js end-of-life warning detected - checking if synthesis actually succeeded..."
                
                # Check if CDK actually generated templates despite the warning
                if ls cdk.out/*.template.json >/dev/null 2>&1 || ls cdk.out/*.template.yaml >/dev/null 2>&1; then
                    echo "✅ CDK synthesis succeeded despite Node.js warnings (CloudFormation templates found)"
                    echo "📋 Node.js warning is informational only - infrastructure is valid"
                    VALIDATION_STATUS="success"
                else
                    echo "❌ CDK synthesis failed - no CloudFormation templates generated"
                    VALIDATION_STATUS="failed"
                fi
            else
                echo "❌ CDK synthesis failed - Infrastructure has real errors"
                VALIDATION_STATUS="failed"
            fi

            # Create artifacts based on validation status
            if [ "$VALIDATION_STATUS" = "success" ]; then
                echo '{"status":"success","execution_mode":"'$EXECUTION_MODE'"}' > validation-results.json
                echo "CDK Synthesis: SUCCESS" > validation-report.txt
            else
                echo '{"status":"failed","execution_mode":"'$EXECUTION_MODE'"}' > validation-results.json
                echo "CDK Synthesis: FAILED" > validation-report.txt
                echo ""
                echo "Error details:"
                head -20 cdk-synth-errors.txt
                exit 1
            fi

            echo ""
            echo "📊 Stage: ValidateInfrastructure - COMPLETED"
            echo "⏱️ Duration: $(date -u +%Y-%m-%dT%H:%M:%SZ)"
      Container:
        Registry: CODECATALYST
        Image: CodeCatalystLinux_x86_64:2024_03
    Outputs:
      Artifacts:
        - Name: validationResults
          Files:
            - "validation-results.json"
            - "validation-report.txt"
            - "cdk-synth-output.txt"
            - "cdk-synth-errors.txt"

  DeployInfrastructure:
    Identifier: aws/build@v1
    DependsOn:
      - ValidateInfrastructure
    Compute:
      Type: EC2
    Environment:
      Connections:
        - Role: CodeCatalystWorkflowDevelopmentRole-AWSCocha
          Name: "142728997126"
      Name: AWS
    Inputs:
      Sources:
        - WorkflowSource
      Artifacts:
        - deploymentContext
        - validationResults
    Configuration:
      Steps:
        - Run: |
            echo "🚀 Infrastructure Deployment"
            echo "============================"
            echo "📅 Stage Start: $(date -u +%Y-%m-%dT%H:%M:%SZ)"
            echo ""

            # Read execution mode from context
            EXECUTION_MODE=$(jq -r '.execution_mode' deployment-context.json)
            SKIP_DEPLOYMENT=$(jq -r '.skip_deployment' deployment-context.json)

            echo "🎯 Mode: $EXECUTION_MODE"
            echo "🔄 Skip Deployment: $SKIP_DEPLOYMENT"
            echo ""

            if [ "$SKIP_DEPLOYMENT" = "true" ]; then
                echo "⏭️ Skipping deployment (validation mode for PR)"
                echo "📋 This is a pull request validation - no actual deployment will occur"
                
                # Create placeholder artifacts for PR validation
                echo '{"status":"skipped","reason":"validation_mode"}' > deployment-results.json
                echo "Deployment: SKIPPED (validation mode)" > deployment-report.txt
                
                echo "✅ Validation pipeline completed successfully"
            else
                echo "🚀 Proceeding with infrastructure deployment..."
                
                # Set up CDK environment
                npm install -g aws-cdk@2.60.0 > /dev/null 2>&1
                python3 -m venv .venv > /dev/null 2>&1
                source .venv/bin/activate
                pip install -r requirements.txt > /dev/null 2>&1

                # Suppress Node.js warnings
                export CDK_DISABLE_VERSION_CHECK=1
                export NODE_NO_WARNINGS=1

                # Deploy infrastructure
                echo "Deploying CDK stacks..."
                if cdk deploy --all --require-approval never > deployment-output.txt 2>deployment-errors.txt; then
                    echo "✅ Infrastructure deployment successful"
                    echo '{"status":"success","execution_mode":"'$EXECUTION_MODE'"}' > deployment-results.json
                    echo "Deployment: SUCCESS" > deployment-report.txt
                else
                    echo "❌ Infrastructure deployment failed"
                    echo '{"status":"failed","execution_mode":"'$EXECUTION_MODE'"}' > deployment-results.json
                    echo "Deployment: FAILED" > deployment-report.txt
                    echo ""
                    echo "Deployment errors:"
                    head -20 deployment-errors.txt
                    exit 1
                fi
            fi

            echo ""
            echo "📊 Stage: DeployInfrastructure - COMPLETED"
            echo "⏱️ Duration: $(date -u +%Y-%m-%dT%H:%M:%SZ)"
      Container:
        Registry: CODECATALYST
        Image: CodeCatalystLinux_x86_64:2024_03
    Outputs:
      Artifacts:
        - Name: deploymentResults
          Files:
            - "deployment-results.json"
            - "deployment-report.txt"
            - "deployment-output.txt"
            - "deployment-errors.txt"